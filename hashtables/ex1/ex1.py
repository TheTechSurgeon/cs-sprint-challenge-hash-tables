def get_indices_of_item_weights(weights, length, limit):
    """
    YOUR CODE HERE
    """
    #firstpass solution
    # for i in weights:
    #     for j in weights:
    #         if(j+i)==limit:
    #             return (j,i)
    # return None
